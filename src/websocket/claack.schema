races(id SERIAL PRIMARY KEY, text varchar(1200) NOT NULL UNIQUE, created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP)
auth(username varchar(15) PRIMARY KEY UNIQUE, pwd varchar(32) NOT NULL, created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP)
rep(username varchar(15) PRIMARY KEY UNIQUE, count int)